# The default keymap for Redox

To install QMK CLI:
`python3 -m pip install --user qmk`

To convert JSON:
`qmk json2c $path_to_json_file > $repo_dir/keyboards/redox/keymaps/rcebulko/keymap.c`

To apply changes:
`qmk flash`

Reset: inner most key pair (L4+L4)
